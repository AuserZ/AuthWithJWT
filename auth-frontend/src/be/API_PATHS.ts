export const DOMAIN_PATH = "http://localhost:8090";

export const API_PATHS = {
    AUTH: {
        LOGIN: "/auth/login",
        LOGOUT: "/auth/logout",
        REGISTER: "/auth/register",
        REFRESH: "/auth/refresh"
    }
}